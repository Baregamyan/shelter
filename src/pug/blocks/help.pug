section#help.help
  .help__wrapper
    h2.title.help__title #{help.title}
    ul.help__list
      each item in help.list
        li.help__item
          h3.help__name #{item.name}
          svg.help__svg(viewBox="0 0 60 60" width=60 height=60)
            use(xlink:href=`#${item.svg.id}`)
